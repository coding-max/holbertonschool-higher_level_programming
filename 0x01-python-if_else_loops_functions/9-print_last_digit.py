#!/usr/bin/python3
def print_last_digit(number):
    if number < 0:
        aux = -number
    
    last_digit = aux % 10
    if number < 0
        last_digit = -last_digit
    
    return(last_digit)
